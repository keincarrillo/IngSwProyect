package com.example.ingswproyect.presentation.theme

import androidx.compose.ui.graphics.Color

// Colores principales
val VerdePrincipal   = Color(0xFF478D4F) // Primary
val VerdeSecundario  = Color(0xFF478D4F) // Secondary (mismo tono por ahora)
val VerdeClaro       = Color(0xFF81C784) // Acentos
val VerdeContenedor  = Color(0xFFF6FBF2) // Superficies claras
val VerdeBorde       = Color(0xFFC1C8BD) // Outline

// Grises y neutros
val GrisClaro        = Color(0xFFE0E0E0)
val GrisMedio        = Color(0xFF9E9E9E)
val GrisCard         = Color(0xFFF3F3F3)
val GrisOscuro       = Color(0xFF868686)

// Complementarios
val Blanco           = Color(0xFFFFFFFF)
val Negro            = Color(0xFF000000)

// Gráfica ritmo cardíaco
val AzulLigero       = Color(0xFF00CFF9)
val VerdeAerobico    = Color(0xFF8D9D3D)
val AmarilloIntensivo= Color(0xFFFFA20D)
val NaranjaAnaerobico= Color(0xFFFF5E3A)
val RojoVOMAx        = Color(0xFFFF1A1A)

// Temático
val MarronKoala      = Color(0xFF5D4037)

// Modo oscuro
val FondoOscuro      = Color(0xFF0E1312)

// Error / eliminar
val RojoClaro        = Color(0xFFEC615B)
